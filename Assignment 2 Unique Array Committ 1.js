function unique (array)
{
    var str=array;
    var unique="";
    for (var x=0;x < str.length;x++)
    {

        if (!unique.array) {
            continue;
        }
        unique += str[x];
    }
    return unique;
}
console.log(unique ("Gizmo Ortiz"));